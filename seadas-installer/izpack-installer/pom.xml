<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>gov.nasa.gsfc.seadas</groupId>
    <artifactId>izpack-installer</artifactId>
    <version>1.2</version>
    <name>SeaDAS Installer</name>
    <packaging>jar</packaging>

    <!-- maven repository where the izpack-maven-plugin and such live -->
    <repositories>
        <repository>
            <id>codehaus-releases</id>
            <url>https://nexus.codehaus.org/content/repositories/releases</url>
        </repository>
    </repositories>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <staging.dir>${project.build.directory}/staging</staging.dir>
        <izpack.version>5.2.4</izpack.version>

        <!--
            installer.descriptor and installer-output-filename are supplied by the
            mac / linux / win profiles below.  There is deliberately no default: a
            build without -P would otherwise silently produce an installer for the
            wrong platform.  Build with one of:

                mvn package -P mac      (macOS, Apple Silicon)
                mvn package -P linux
                mvn package -P win
        -->
    </properties>

    <profiles>
        <profile>
            <id>mac</id>
            <properties>
                <installer.descriptor>install-for-mac.xml</installer.descriptor>
                <installer-output-filename>seadas-installer-macos-aarch64</installer-output-filename>
            </properties>
        </profile>

        <profile>
            <id>linux</id>
            <properties>
                <installer.descriptor>install-for-linux.xml</installer.descriptor>
                <installer-output-filename>seadas-installer-linux-x64</installer-output-filename>
            </properties>
        </profile>

        <profile>
            <id>win</id>
            <properties>
                <installer.descriptor>install-for-windows.xml</installer.descriptor>
                <installer-output-filename>seadas-installer-windows-x64</installer-output-filename>
            </properties>
            <build>
                <plugins>
                    <!-- Launch4j: wrap the installer JAR into a Windows EXE.
                         Only meaningful for the Windows build. -->
                    <plugin>
                        <groupId>com.akathist.maven.plugins.launch4j</groupId>
                        <artifactId>launch4j-maven-plugin</artifactId>
                        <version>2.6.0</version>
                        <executions>
                            <execution>
                                <id>l4j-gui</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>launch4j</goal>
                                </goals>
                                <configuration>
                                    <headerType>gui</headerType>

                                    <!-- This now matches the IzPack output exactly -->
                                    <jar>${project.build.directory}/${installer-output-filename}.jar</jar>

                                    <outfile>target/${installer-output-filename}.exe</outfile>
                                    <classPath>
                                        <mainClass>com.izforge.izpack.installer.bootstrap.Installer</mainClass>
                                    </classPath>
                                    <jre>
                                        <requires64Bit>true</requires64Bit>
                                        <minVersion>21.0.0</minVersion>
                                        <!-- relaxed so future 21.x builds still work -->
                                        <maxVersion>21.99.99</maxVersion>
                                    </jre>
                                    <versionInfo>
                                        <fileVersion>1.0.0.0</fileVersion>
                                        <txtFileVersion>${project.version}</txtFileVersion>
                                        <fileDescription>${project.name}</fileDescription>
                                        <copyright>C</copyright>
                                        <productVersion>1.0.0.0</productVersion>
                                        <txtProductVersion>1.0.0.0</txtProductVersion>
                                        <productName>${project.name}</productName>
                                        <internalName>SeaDAS</internalName>
                                        <originalFilename>${installer-output-filename}.exe</originalFilename>
                                    </versionInfo>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

    <developers>
        <developer>
            <id>aynur</id>
            <name>Aynur Abdurazik</name>
            <email>aynur.abdurazik@nasa.gov</email>
            <organization>NASA Ocean Ecology Lab</organization>
            <timezone>-4</timezone>
        </developer>
    </developers>

    <build>
        <defaultGoal>package</defaultGoal>

        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.codehaus.izpack</groupId>
                    <artifactId>izpack-maven-plugin</artifactId>
                    <version>${izpack.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-clean-plugin</artifactId>
                    <version>2.4.1</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-deploy-plugin</artifactId>
                    <version>2.6</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-install-plugin</artifactId>
                    <version>2.3.1</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-site-plugin</artifactId>
                    <version>2.3</version>
                </plugin>
            </plugins>
        </pluginManagement>

        <plugins>

            <!-- copy all resources to the staging directory. -->
            <plugin>
                <artifactId>maven-resources-plugin</artifactId>
                <version>3.3.1</version>
                <executions>
                    <execution>
                        <id>copy-resources</id>
                        <phase>validate</phase>
                        <goals>
                            <goal>copy-resources</goal>
                        </goals>
                        <configuration>
                            <outputDirectory>${staging.dir}</outputDirectory>
                            <resources>

                                <resource>
                                    <directory>../../../snap-desktop/snap-application/target/snap</directory>
                                    <includes>
                                        <include>**/*</include>
                                    </includes>
                                    <targetPath>../../src/main/izpack/packs/snap</targetPath>
                                    <filtering>false</filtering>
                                </resource>

                                <resource>
                                    <directory>../../../optical-toolbox/opttbx-kit/target/netbeans_clusters/opttbx</directory>
                                    <includes>
                                        <include>**/*</include>
                                    </includes>
                                    <targetPath>../../src/main/izpack/packs/opttbx</targetPath>
                                    <filtering>false</filtering>
                                </resource>

                                <resource>
                                    <directory>../../../seadas-toolbox/seadas-kit/target/netbeans_clusters/seadas</directory>
                                    <includes>
                                        <include>**/*</include>
                                    </includes>
                                    <targetPath>../../src/main/izpack/packs/seadas</targetPath>
                                    <filtering>false</filtering>
                                </resource>

                                <resource>
                                    <directory>src/main/izpack</directory>
                                    <includes>
                                        <include>**/*</include>
                                    </includes>
                                    <filtering>false</filtering>
                                </resource>

                            </resources>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <artifactId>maven-antrun-plugin</artifactId>
                <version>1.8</version>
                <executions>
                    <execution>
                        <phase>validate</phase>
                        <goals>
                            <goal>run</goal>
                        </goals>
                        <configuration>
                            <target>
                                <concat destfile="${staging.dir}/packs/snap/etc/snap.properties" append="true">
                                    ${line.separator}snap.context.application.name=SeaDAS
                                </concat>

                                <concat destfile="${staging.dir}/packs/snap/etc/snap.clusters" append="true">
                                    optical-toolbox${line.separator}seadas-toolbox
                                </concat>

                                <concat destfile="${staging.dir}/packs/snap/etc/snap.conf" append="true">
                                    extra_clusters=${extra_clusters}
                                </concat>
                            </target>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <artifactId>maven-dependency-plugin</artifactId>
                <executions>
                    <execution>
                        <!-- copy izpack custom (custom panels, etc.) jars to izpack staging custom -->
                        <id>copy-izpack-dependencies</id>
                        <phase>prepare-package</phase>
                        <goals>
                            <goal>copy-dependencies</goal>
                        </goals>
                        <configuration>
                            <outputDirectory>${staging.dir}/custom</outputDirectory>
                            <excludeTransitive>false</excludeTransitive>
                            <stripVersion>true</stripVersion>
                            <overWriteReleases>true</overWriteReleases>
                            <overWriteSnapshots>true</overWriteSnapshots>
                            <overWriteIfNewer>true</overWriteIfNewer>
                            <!-- IMPORTANT: this causes *only* our custom panels to be copied -->
                            <includeArtifactIds>izpack-panels</includeArtifactIds>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <!-- IzPack: build the installer JAR -->
            <plugin>
                <groupId>org.codehaus.izpack</groupId>
                <artifactId>izpack-maven-plugin</artifactId>
                <version>${izpack.version}</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>izpack</goal>
                        </goals>
                        <configuration>
                            <!-- base for relative paths in izpack descriptor -->
                            <baseDir>${staging.dir}</baseDir>
                            <installFile>${staging.dir}/${installer.descriptor}</installFile>

                            <!-- Output: target/seadas-installer.jar -->
                            <outputDirectory>${project.build.directory}</outputDirectory>
                            <finalName>${installer-output-filename}</finalName>
                            <!-- no classifier, so the file name is exactly finalName.jar -->
                        </configuration>
                    </execution>
                </executions>
            </plugin>

        </plugins>
    </build>
</project>
